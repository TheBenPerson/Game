Improve and fix audio (opens file and new thread for every sound)
Trim audio
Inspect memory increase when scrolling through buttons
Decide max size of NodeList
Fix crash on fullscreen toggle
Inspect what happens when listeners removed while looping through them
dlsym searches dependancies. Very problematic when module has no depends symbol, but it's dependancies do.
