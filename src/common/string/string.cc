#include <stdlib.h>
#include <string.h>

#include "string.hh"

/*namespace String {

	char *cat(char *a, char *b) {

		unsigned int len = strlen(a);

		char *dest = (char*) malloc(len + strlen(b) + 1);
		strcpy(dest, a);
		strcpy(dest + len, b);

		return dest;

	}

}*/
