Trim audio
Inspect memory increase when scrolling through buttons
Fix crash on fullscreen toggle
Inspect what happens when listeners removed while looping through them
Even though client looses connection, client w/ username still exists
Give players their actual names
Add life
Fix multi worlds
Change packet size so that size reflects the size of data minus packet id
Fix client timeout
